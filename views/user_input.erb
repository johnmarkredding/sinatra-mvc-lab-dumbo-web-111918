<h2>Instructions</h2>
<ol>
  <li>Build a form to take user input in user_input.erb. Show that form using a GET request at /.</li>
  <li>Create a POST method in your controller (app.rb) to receive your form's params.</li>
  <li>Build a PigLatinizer model (in your models directory) that converts a string into pig latin.</li>
  <li>In your application controller, create an instance of the PigLatinizer class to convert your user input to Pig Latin.</li>
  <li>Use ERB within a new view to display the final pig latin string to the user.</li>
</ol>

<form method="POST" action="/piglatinize">
  <input type="textarea" name="text">
  <input type="submit" id="Submit">
</form>